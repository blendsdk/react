{
    "name": "root",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build_all": "lerna clean -y && lerna bootstrap && lerna link && lerna run build --stream",
        "publish_patch": "yarn build_all && lerna publish patch"
    },
    "devDependencies": {
        "@types/jest": "^24.0.18",
        "@types/node": "^12.7.5",
        "@types/superagent": "^4.1.3",
        "@types/supertest": "^2.0.8",
        "jest": "^24.9.0",
        "lerna": "^3.16.4",
        "superagent": "^5.1.0",
        "supertest": "^4.0.2",
        "ts-jest": "^24.0.2",
        "tslint": "^5.20.0",
        "typescript": "^3.6.3"
    }
}
